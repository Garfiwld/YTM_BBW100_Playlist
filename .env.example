# Browser auth needs no secrets in .env. This file only overrides paths, if you want.
# Copy to .env to use.

# Path to the ytmusicapi browser-auth file (default: ./browser.json)
#YTM_BROWSER_FILE=browser.json
